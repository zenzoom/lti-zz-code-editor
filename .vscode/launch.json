{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "XLaunch Chrome",
        "request": "launch",
        "type": "chrome",
        "url": "http://localhost:5173/", 
        "webRoot": "${workspaceFolder}/src/"
      },
      {
      // Debugging...
      // 1) In "Main.svelte" and "index.html" uncomment the Debugging mocked parameters
      // 2) Run "npm run dev". Note the port number and adjust the following launch config.
      // Note: If breakpoints don't set then change "webRoot" below.
      // ///Debugging...
        "name": "Launch Edge",
        "request": "launch",
        "type": "msedge",
        "url": "http://localhost:5173/", 
        "webRoot": "${workspaceFolder}/src"
      }
     
    ]
  }
  